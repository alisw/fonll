      subroutine elpdf_acf(Q2,X,FX,NF)
      real * 4 q2,x,fx(*)
      write(*,*) 'elpdf_acf not here '
      stop
      end

      subroutine elpdf_afg(Q2,X,FX,NF)
      real * 4 q2,x,fx(*)
      write(*,*) 'elpdf_afg not here '
      stop
      end

      subroutine fxdflm1(X,Q2,PART,FX)
      real x,q2,fx
      character * 2 part
      write(*,*) ' fxdflm1 not here '
      stop
      end

      subroutine fxdflm2(X,Q2,PART,FX)
      real x,q2,fx
      character * 2 part
      write(*,*) ' fxdflm2 not here '
      stop
      end

      subroutine fxdflm3(X,Q2,PART,FX)
      real x,q2,fx
      character * 2 part
      write(*,*) ' fxdflm3 not here '
      stop
      end

      subroutine elpdf_dg(Q2,X,FX,NF)
      real * 4 q2,x,fx
      write(*,*) 'elpdf_dg not here '
      stop
      end

      subroutine elpdf_lac1(Q2,X,FX,NF)
      real * 4 q2,x,fx(*)
      write(*,*) 'elpdf_lac1 not here '
      stop
      end

      subroutine elpdf_lac2(Q2,X,FX,NF)
      real * 4 q2,x,fx(*)
      write(*,*) 'elpdf_lac2 not here '
      stop
      end

      subroutine elpdf_lac3(Q2,X,FX,NF)
      real * 4 q2,x,fx(*)
      write(*,*) 'elpdf_lac3 not here '
      stop
      end

      subroutine elpdf_gs(Q2,X,FX,NF)
      real * 4 q2,x,fx(*)
      write(*,*) 'elpdf_gs not here '
      stop
      end

      subroutine elpdf_grv(Q2,X,FX,NF)
      real * 4 q2,x,fx(*)
      write(*,*) 'elpdf_grv not here '
      stop
      end

      function ran(iseed)
      ran=0.d0
      stop
      return
      end
